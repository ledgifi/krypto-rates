{
  "name": "@raptorsystems/krypto-rates-python",
  "version": "2.0.0-alpha.0",
  "description": "Krypto Rates Python Client",
  "author": {
    "name": "Raptor Systems SpA",
    "email": "raptor@raptorsystems.cl"
  },
  "license": "MIT",
  "private": true,
  "scripts": {
    "lint": "poetry run flake8 --exit-zero",
    "clean": "rimraf dist build *.egg-info",
    "prebuild": "yarn clean",
    "build:python": "poetry run python setup.py --quiet sdist bdist_wheel --universal",
    "upload": "poetry run twine upload dist/*"
  }
}
